# Thread_Concurrency
자바의 스레드와 동시성에 대한 이론 및 코드

---

## 💡 프로세스, 스레드, 멀티태스킹

### 🔹 프로세스 (Process)
- **정의:** 실행 환경과 자원을 제공하는 컨테이너
- **특징:** 각 프로세스는 독립적인 메모리 공간을 가짐
- **스레드 관계:** 한 프로세스 안에 여러 스레드가 존재하며, 메모리를 공유 가능

**예시:**
> **프로세스:** 워드프로세스  
> - 스레드 1: 문서 자동 저장  
> - 스레드 2: 문서 편집  
> - 스레드 3: 맞춤법 검사  

---

### 🔹 스레드 (Thread)
- 프로세스 내부에서 실제 작업을 수행하는 **실행 단위**  
- 스레드끼리는 **공유 메모리**를 통해 효율적 통신 가능  
- 동기화 문제(교착 상태 등) 주의 필요

---

### 🔹 멀티태스킹 (Multitasking)
- CPU가 여러 작업(프로세스)을 **빠르게 번갈아 실행**  
- 사람 눈에는 동시에 수행되는 것처럼 보임

---

### 🔹 멀티프로세싱 (Multiprocessing)
- CPU 코어가 여러 개일 때, 각 코어가 서로 다른 프로세스나 스레드를 **동시에 실행**  
- 진정한 병렬(Parallel) 실행 가능

---

## ⚙️ 스케줄링 (Scheduling)

### 🔸 스케줄링 큐
- 운영체제 내부에서 CPU 할당 순서를 결정  
- CPU는 스케줄 큐에 있는 작업을 순서대로 가져와 실행

### 🔸 멀티코어 스케줄링
- CPU 코어 2개 이상 → 각 코어가 다른 스레드 동시 실행 가능

---

## 🧩 컨텍스트 스위칭 (Context Switching)
- **정의:** CPU가 현재 실행 중인 작업 상태를 저장하고 다른 작업으로 전환하는 과정  
- **원리:** 레지스터, 스택 포인터 등 상태 저장/복원  
- **주의:** 너무 자주 전환하면 성능 저하

**예시:**  
> 1~5000 계산 후 5000~10000 계산 결과 합치기보다,  
> 1~10000을 한 번에 계산하는 것이 더 효율적  
> 이유: 두 작업으로 나누면 컨텍스트 스위칭 비용이 발생

---

## 🔹 스레드 작업 유형

| 유형 | 설명 | 예시 |
|------|------|------|
| CPU Bound Task | CPU 연산 중심 작업 | 데이터 압축, 이미지 처리, 수학 계산 |
| I/O Bound Task | 입출력 대기 시간이 많은 작업 | DB 조회, 파일 읽기, 네트워크 요청 |

> 💡 대부분의 자바 웹 애플리케이션은 **I/O Bound 작업**이 많음

---

## 🔹 자바 웹 애플리케이션과 스레드 구조
- 사용자 요청 1개당 1개의 스레드 생성
- 예: 4명의 사용자 요청 → Thread-1, Thread-2, Thread-3, Thread-4

---

# 🧠 자바 메모리 구조

## 1️⃣ 메서드 영역
- 프로그램 실행에 필요한 **공통 데이터 관리**  
- 모든 스레드가 **공유**  
- 포함: 클래스 정보, 필드/메서드 코드, static 변수, 런타임 상수 풀

## 2️⃣ 스택 영역
- **스레드별로 독립적**  
- 스택 프레임: 지역 변수, 연산 결과, 메서드 호출 정보 포함

## 3️⃣ 힙 영역
- 객체와 배열 저장  
- 모든 스레드가 **공유**  
- 참조되지 않으면 **GC**에 의해 제거

**정리:**

| 영역 | 역할 | 스레드 관계 |
|------|------|------------|
| 메서드 | 클래스, static, 상수 | 공유 |
| 스택 | 메서드 실행, 지역 변수 | 스레드별 독립 |
| 힙 | 객체, 배열 | 공유 |

---

# Java 스레드(Thread)

## 1. 스레드 특징
- 실행 순서 예측 불가  
- 다중 CPU 코어 → 동시에 실행 가능  
- 하나의 CPU → 시간 나누어 실행  
- **순서와 실행 시간 보장 불가**

---

## 2. `start()` vs `run()`
- `start()`: 스택 공간 할당 후 별도 스레드에서 `run()` 실행  
- 직접 `run()` 호출 시 main 스레드에서 실행

---

## 3. 스레드 종류

### 사용자 스레드
- 주요 작업 수행  
- 종료 전까지 JVM 종료되지 않음

### 데몬 스레드
- 백그라운드 작업 수행  
- 사용자 스레드 종료 시 자동 종료

---

## 4. 스레드 생성

### 4.1 Thread 상속
- 장점: run() 재정의만 하면 됨  
- 단점: 상속 제한, 유연성 부족

### 4.2 Runnable 구현
- 장점: 상속 자유, 코드 분리, 객체 공유 가능  
- 실무 권장

---

## 🧵 Thread 기본 정보

```java
Thread mainThread = Thread.currentThread();
log("mainThread = " + mainThread);
log("mainThread.threadId() = " + mainThread.threadId());
log("mainThread.getName() = " + mainThread.getName());
log("mainThread.getPriority() = " + mainThread.getPriority());
log("mainThread.getThreadGroup() = " + mainThread.getThreadGroup());
log("mainThread.getState() = " + mainThread.getState());

Thread myThread = new Thread(new HelloRunnable(), "myThread");
log("myThread = " + myThread);
log("myThread.threadId() = " + myThread.threadId());
log("myThread.getName() = " + myThread.getName());
log("myThread.getPriority() = " + myThread.getPriority());
log("myThread.getThreadGroup() = " + myThread.getThreadGroup());
log("myThread.getState() = " + myThread.getState());

상태 예시

mainThread → RUNNABLE

myThread → NEW

정리:

Thread: 생성, 상태 관리, 그룹 관리 가능

NEW → start() 호출 전 상태

RUNNABLE → 실행 중 상태
```

---
